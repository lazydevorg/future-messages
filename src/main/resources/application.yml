logging:
  level:
    org.lazydev.futuremessages: DEBUG

spring:
  datasource:
    url: jdbc:h2:mem:future-messages;MVCC=TRUE

  quartz:
    job-store-type: jdbc
    jdbc:
      initialize-schema: always